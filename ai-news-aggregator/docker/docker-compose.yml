version: "3.9"
services:
  app:
    build: ..
    env_file:
      - ../.env.example
    depends_on:
      - db
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: news
      POSTGRES_PASSWORD: news
      POSTGRES_DB: news
    ports:
      - "5432:5432"
